# SPDX-FileCopyrightText: 2025 SAP SE or an SAP affiliate company
#
# SPDX-License-Identifier: Apache-2.0

[hermes]
# Storage Backend Selection (choose one):
# - "elasticsearch" (default) - For Elasticsearch 7.x clusters
# - "opensearch" - For OpenSearch 1.x/2.x clusters
# - "mock" - For testing without a real backend
#
# If omitted, defaults to "elasticsearch" for backward compatibility.
#storage_driver = "elasticsearch"
#storage_driver = "opensearch"
#storage_driver = "mock"

# Identity Backend Selection:
#keystone_driver = "mock"

PolicyFilePath = "etc/policy.json"

# Elasticsearch Configuration
# Only needed if storage_driver = "elasticsearch" (or using default)
# Credentials can be set via environment variables:
#   HERMES_ES_USERNAME, HERMES_ES_PASSWORD
[elasticsearch]
url = "http://localhost:9200"
#username = ""
#password = ""
#max_result_window = "20000"

# OpenSearch Configuration
# Only needed if storage_driver = "opensearch"
# Credentials can be set via environment variables:
#   HERMES_OS_USERNAME, HERMES_OS_PASSWORD
[opensearch]
url = "http://localhost:9200"
#username = ""
#password = ""
#max_result_window = "20000"

[keystone]
auth_url = "https://identity-3.staging.cloud.sap/v3/"
#auth_url = "https://keystone.example.com/v3"
username = "hermes"
password = "C0nrad^"
user_domain_name = "Default"
project_name = "service"
project_domain_name = "Default"
#token_cache_time = 900
#memcached_servers = memcached.example.com:11211
